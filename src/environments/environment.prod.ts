// Production environment.
// Set this to your deployed Spring Boot API URL (Render, etc.).
export const environment = {
  production: true,
  apiBase: '' // e.g. 'https://kgn-store-api.onrender.com'
};
